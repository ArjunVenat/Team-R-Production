import graphNode from "./graph.ts";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
class minHeap {
  constructor() {
    // do something idk
  }

  // Public methods
  /* eslint-disable */
  insert(node: graphNode, weight: number) {}

  delete(node: graphNode) {}

  // Returns the value at the top but does not remove it
  peek(): graphNode {
    // TODO: make this not a dummy return
    return new graphNode();
  }

  // Returns the value at the top AND removes it
  pop(): graphNode {
    // TODO: make this not a dummy return
    return new graphNode();
  }
  /* eslint-enable */
}
